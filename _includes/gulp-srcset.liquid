{% capture blank %}
    {% assign src = include.src %}
    {% if include.bp_list %}
        {% assign bp_list = include.bp_list %}
    {% else %}
        {% assign bp_list = site.image_bp | reverse %}
    {% endif %}
    {% assign srcset = '' %}
    {% for bp in bp_list %}
        {% assign x = bp | map: 'x' %}
        {% assign y = bp | map: 'y' %}
        {% capture suffix %}-{{ x }}w{% endcapture %}
        {% capture new_src %}{% include file-suffix.liquid file_name=src suffix=suffix %} {{ x }}w{% unless forloop.last %}, {% endunless %}{% endcapture %}
        {% assign srcset = srcset | append: new_src %}
    {% endfor %}
    {% assign return = srcset %}
{% endcapture %}{% if jekyll.environment == 'netlify' %}{{ return | strip_newlines }}{% endif %}
